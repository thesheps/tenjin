service: tenjin

frameworkVersion: "3"

provider:
  name: aws
  runtime: nodejs12.x
  lambdaHashingVersion: "20201221"

functions:
  auth:
    handler: src/handler.auth
    events:
      - http:
          path: /
          method: get
